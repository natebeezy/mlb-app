services:
  - type: web
    name: mlb-app-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn mlb_betting_backend:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
